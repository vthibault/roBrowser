Usage of utils programs/scripts

Webserver
-----------------
Starting a webserver that will server the current folder at port 8080
```
$ ./webserver -p 80 -d ./
```

To print all available parameters use
```
$ ./webserver -h
```

